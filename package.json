{
  "name": "assignment-upload-server",
  "version": "1.0.0",
  "description": "Server for uploading assignments to Google Drive",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "googleapis": "^126.0.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "vercel": "^32.5.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 